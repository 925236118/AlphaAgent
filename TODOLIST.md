# TODOLIST

## 简介
此处列出了现在已有的和后续需要开发的功能


## 工具
### 已完成
| #   | 工具名称                     | 分类       | 简要描述                            |
| --- | ---------------------------- | ---------- | ----------------------------------- |
| 1   | `get_project_info`           | 查询       | 获取 Godot 引擎、系统和项目配置信息 |
| 2   | `get_editor_info`            | 查询       | 获取编辑器当前打开的场景和脚本状态  |
| 3   | `get_project_file_list`      | 查询       | 获取项目中所有文件及其 UID 列表     |
| 4   | `read_file`                  | 文件操作   | 读取指定文件的内容                  |
| 5   | `write_file`                 | 文件操作   | 写入或覆盖文件内容                  |
| 6   | `create_folder`              | 文件操作   | 创建文件夹                          |
| 7   | `get_class_doc`              | 查询       | 获取 Godot 原生类的文档信息         |
| 8   | `add_script_to_scene`        | 场景操作   | 将脚本加载到场景节点上              |
| 9   | `sep_script_to_scene`        | 场景操作   | 将脚本从场景节点上分离              |
| 10  | `get_image_info`             | 查询       | 获取图片文件信息                    |
| 11  | `set_singleton`              | 配置       | 设置或删除项目自动加载脚本或场景    |
| 12  | `check_script_error`         | 调试       | 检查脚本中的语法错误                |
| 13  | `open_resource`              | 编辑器操作 | 使用编辑器打开资源文件              |
| 14  | `update_script_file_content` | 编辑器操作 | 使用编辑器编辑脚本内容 |
| 15  | `update_scene_node_property` | 编辑器操作 | 设置某个场景中的某个节点的某个属性为某个值 |
| 16  | `set_resource_property`      | 编辑器操作 | 将资源挂载到某个场景的某个属性，支持嵌套资源 |
| 17  | `execute_command`            | 命令行操作 | 允许agent直接在项目目录下调用命令行工具 |
| 18  | `editor_script_feature`      | 编辑器操作 | 编写脚本可以直接运行的脚本，并运行（暂不开放agent调用） |


### 待完成
- 操作AnimationPlayer中的动画资源
	- 加载动画资源后，根据ai结果分别添加动画、轨道。ai结果应是一个json数组，这样可以一次返回，多次设置。
- 操作TileSet中的数据
	- 根据ai返回的json数组数据，设置瓦片的数据
- 获取TileSet中的数据
	- 可筛选图集
	- 将图集中的瓦片信息、坐标、大小、物理、渲染、光照、导航、



## 交互

### 已完成
- 添加思考模式
- 将设置改为全局设置
- 添加设置项
	- 发送后清空输入
	- secret key
	- 是否自动展开思考
	- 添加引用后不自动填写文件名


### 待完成
- 添加记忆管理面板
- 添加设置项
	暂无
- 优化Ask模式为可以调用工具但不可写文件的模式
- 引用时可以使用快捷键添加引用的代码段，将位置和代码路径一起发送给ai。
- 输入位置添加快捷键快速发送
- 如果没有任何用户输入，则不应可以发送消息
- 为对话添加todo list功能，让其可以先规划任务、然后再依次完成
- 为对话添加总结功能，总结后可开启一段新对话，并重新发送系统提示词
- 添加引用后应在当前光标位置立刻添加一个文件名字符串，避免用户再次输入。使用TextEdit.insert_text_at_caret(text: String, caret_index: int = -1)
- 添加DeepSeek报错后的错误信息展示
	- 完成error信号
	- 需要添加反馈
- 添加用户中断AI请求功能
- 修复历史记录项按钮悬停向下移动鼠标会导致按钮一直出现问题
- 添加作者页面跳转
- 添加从某个位置重新开始
- 添加修改的文件列表，以及撤回功能
- 
